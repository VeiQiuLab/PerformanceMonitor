<Window x:Class="PerformanceMonitor.Views.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Settings" Width="760" Height="700" MinWidth="680" MinHeight="620"
        Background="{StaticResource WindowBrush}" Foreground="{StaticResource PrimaryTextBrush}"
        WindowStartupLocation="CenterOwner" ResizeMode="CanResizeWithGrip">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Text="General" FontSize="19" FontWeight="SemiBold" />
        <Border Grid.Row="1" Background="{StaticResource PanelBrush}" BorderBrush="{StaticResource PanelBorderBrush}"
                BorderThickness="1" CornerRadius="8" Padding="16" Margin="0,12,0,18">
            <StackPanel>
                <CheckBox Content="Start with Windows" IsChecked="{Binding StartWithWindows}"
                          Foreground="{StaticResource PrimaryTextBrush}" />
                <TextBlock Text="Starts minimized in the system tray with highest privileges. No UAC prompt appears at sign-in."
                           Foreground="{StaticResource SecondaryTextBrush}" FontSize="11" Margin="24,7,0,0"
                           TextWrapping="Wrap" />
            </StackPanel>
        </Border>

        <TextBlock Grid.Row="2" Text="LAN Server" FontSize="19" FontWeight="SemiBold" />
        <Border Grid.Row="3" Background="{StaticResource PanelBrush}" BorderBrush="{StaticResource PanelBorderBrush}"
                BorderThickness="1" CornerRadius="8" Padding="16" Margin="0,12,0,18">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" /><RowDefinition Height="Auto" /><RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                <CheckBox Content="Enable LAN Monitoring" IsChecked="{Binding LanServer.Enabled}"
                          Foreground="{StaticResource PrimaryTextBrush}" Margin="0,0,0,13" />
                <Grid Grid.Row="1" Margin="0,0,0,11">
                    <Grid.ColumnDefinitions><ColumnDefinition Width="130" /><ColumnDefinition Width="130" /><ColumnDefinition /></Grid.ColumnDefinitions>
                    <TextBlock Text="Listen Port" VerticalAlignment="Center" Foreground="{StaticResource SecondaryTextBrush}" />
                    <TextBox Grid.Column="1" Text="{Binding LanServer.Port, UpdateSourceTrigger=PropertyChanged}" />
                </Grid>
                <Grid Grid.Row="2">
                    <Grid.ColumnDefinitions><ColumnDefinition Width="130" /><ColumnDefinition /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions>
                    <TextBlock Text="Access Token" VerticalAlignment="Center" Foreground="{StaticResource SecondaryTextBrush}" />
                    <TextBox Grid.Column="1" Text="{Binding LanServer.AccessToken, UpdateSourceTrigger=PropertyChanged}" />
                    <Button Grid.Column="2" Content="Regenerate" Margin="10,0,0,0" Click="OnRegenerateToken" />
                </Grid>
            </Grid>
        </Border>

        <Grid Grid.Row="4">
            <Grid.RowDefinitions><RowDefinition Height="Auto" /><RowDefinition Height="*" /></Grid.RowDefinitions>
            <Grid>
                <Grid.ColumnDefinitions><ColumnDefinition /><ColumnDefinition Width="Auto" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions>
                <TextBlock Text="Remote Devices" FontSize="19" FontWeight="SemiBold" VerticalAlignment="Center" />
                <Button Grid.Column="1" Content="Add" Padding="14,6" Click="OnAddRemote" />
                <Button Grid.Column="2" Content="Remove" Padding="14,6" Margin="8,0,0,0" Click="OnRemoveRemote" />
            </Grid>
            <DataGrid x:Name="RemoteGrid" Grid.Row="1" ItemsSource="{Binding RemoteDevices}" Margin="0,12,0,0"
                      AutoGenerateColumns="False" CanUserAddRows="False" HeadersVisibility="Column"
                      Background="{StaticResource PanelBrush}" Foreground="{StaticResource PrimaryTextBrush}"
                      BorderBrush="{StaticResource PanelBorderBrush}" GridLinesVisibility="Horizontal"
                      RowBackground="#191D24" AlternatingRowBackground="#171B21" HorizontalGridLinesBrush="#2B3039">
                <DataGrid.ColumnHeaderStyle>
                    <Style TargetType="DataGridColumnHeader">
                        <Setter Property="Background" Value="#242A33" />
                        <Setter Property="Foreground" Value="{StaticResource PrimaryTextBrush}" />
                        <Setter Property="BorderBrush" Value="#39414C" />
                        <Setter Property="Padding" Value="8,6" />
                    </Style>
                </DataGrid.ColumnHeaderStyle>
                <DataGrid.CellStyle>
                    <Style TargetType="DataGridCell">
                        <Setter Property="Foreground" Value="{StaticResource PrimaryTextBrush}" />
                        <Setter Property="BorderBrush" Value="#2B3039" />
                        <Setter Property="Padding" Value="4" />
                    </Style>
                </DataGrid.CellStyle>
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Display Name" Binding="{Binding DisplayName, UpdateSourceTrigger=PropertyChanged}" Width="1.2*" />
                    <DataGridTextColumn Header="Host / IP" Binding="{Binding Host, UpdateSourceTrigger=PropertyChanged}" Width="1.4*" />
                    <DataGridTextColumn Header="Port" Binding="{Binding Port, UpdateSourceTrigger=PropertyChanged}" Width="80" />
                    <DataGridTextColumn Header="Access Token" Binding="{Binding AccessToken, UpdateSourceTrigger=PropertyChanged}" Width="1.8*" />
                </DataGrid.Columns>
            </DataGrid>
        </Grid>

        <Grid Grid.Row="5" Margin="0,20,0,0">
            <Grid.ColumnDefinitions><ColumnDefinition /><ColumnDefinition Width="Auto" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions>
            <TextBlock x:Name="ValidationMessage" Foreground="{StaticResource OfflineBrush}" VerticalAlignment="Center" TextWrapping="Wrap" />
            <Button Grid.Column="1" Content="Cancel" IsCancel="True" MinWidth="88" />
            <Button Grid.Column="2" Content="Save" MinWidth="88" Margin="10,0,0,0" Click="OnSave" />
        </Grid>
    </Grid>
</Window>
